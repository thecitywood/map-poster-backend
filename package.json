{
  "name": "map-poster-backend",
  "version": "1.0.0",
  "description": "Backend for Map Poster project (uses PostgreSQL via Neon)",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "pg": "^8.11.5"   // paczka dla PostgreSQL (Neon)
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "engines": {
    "node": ">=18.x"
  }
}
